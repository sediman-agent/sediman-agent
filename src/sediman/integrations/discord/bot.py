"""Discord bot client — managed by DiscordIntegration.listen().

The listen() method in __init__.py starts a discord.py gateway client
for receiving inbound commands (e.g. ``!help``). This module is reserved
for future extraction of the bot logic should it grow beyond the current
inline implementation.
"""
